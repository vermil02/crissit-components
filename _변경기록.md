# 변경 기록

컴포넌트 값이 언제 왜 바뀌었는지만 적는다. **읽을 문서는 `README.md` 와 `카탈로그.html` 이다** —
여기는 되돌아볼 일이 있을 때만 본다.

## 2026-09-07 · 피그마 REST 덤프(f2h)로 전수 재검증

MCP(`get_design_context`)로는 못 읽던 값 — 오버레이 농도, 그라디언트 핸들,
boolean 연산의 컷 크기, 컴포넌트 세트의 변형 목록 — 이 덤프에 다 있어서 전부 대조했다.
근거 파일: `~/Downloads/{가이드,Content,Navigation,Action}.f2h.json`

### 바로잡은 값 13건

MCP로 읽었을 때 값이 안 나와 추정으로 채웠거나, 아예 틀렸던 것들이다.
피그마 REST 문서 전체(f2h 덤프)와 대조해 **전부 실측값으로 교체**했다.

| 항목 | 전 (추정·오류) | 후 (실측) |
|---|---|---|
| hover 막 농도 | 검정·흰색 모두 8% | **검정 8% · 흰색 12%** — 막 색에 따라 다르다 |
| Footer White 열 제목·법인정보 | `#7c8288` | **`#a3a8ad`** (Label/Normal/Assistive) |
| Footer CTA 배경 | 연보라→흰색, 빛 번짐 생략 | Black `#151617→#000` · White `#f5f6f7→#fff` + 상단 268px 보라 빛 번짐 40% |
| Footer CTA 제목 | 한 색 | 첫 줄이 한 단계 연하다 (Black `#f5f6f7`/`#fafafa`, White `#414549`/`#151617`) |
| Content Badge | 2단 | **3단** lg 4/8·16 · md 3/6·14 · sm 2/6·12 (피그마 이름과 일치) |
| Board list 태블릿·모바일 | 비례로 축소한 추정 | 전 항목 실측. 특히 Common 가로형 이미지가 **68×40**(추정 120×84), Main 태블릿 이미지 **480**(추정 280) |
| Main 카드 Progress | 모양·동작 모두 없었다 | 테두리를 상단 중앙에서 시계방향으로 **연속으로** 채운다(한 바퀴 8초) → 모양은 `.bl-prog`(conic-gradient 마스크 + `@property` 각도), 동작은 `js/boardlist-main.js`. 피그마 목업 4장은 피그마가 움직임을 못 그려서 쓴 표현 방식이라 보고 코드에는 프레임을 남기지 않았다 |
| Footer 태블릿·모바일 | 여백·열 배치 추정 | 구조가 아예 다르다 — 세로로 쌓이고 메뉴 4열이 **2×2**(열이 각각 절반), 위 32 아래 48, 로고 124×48 |
| Footer CTA 태블릿·모바일 | 왼쪽 정렬 유지 | 제목·본문·버튼이 모두 **가운데 정렬**, 위 64 아래 32 |
| GNB 햄버거 크기 | 태블릿·모바일 모두 20 | **태블릿 24 · 모바일 20** |
| 푸터 CTA 버튼 라운드 | `Round=Brand` | **`Round=Normal`** — 2026-09-07 재내보냄에서 바뀌었다 |
| 메가메뉴 목록 항목 | hover 만 대충 | `Navigation/LNB/List` 신규 컴포넌트 반영 — **Active(굵어짐)** 와 **외부 링크(↗)** 가 정식 상태 |
| 메가메뉴 어두운 배경 글자 | `#c9ccd0` | **`#e4e6e8`** (Label/Inverse/Alternative) |

추정이었지만 **맞았던** 것 — Round=Brand 컷 6px(세 크기 모두) · GNB Scroll 구분선 색 ·
배지 면 `#e4e6e8` · Chip·Pagination·Dropdown 전 수치 · GNB 데스크톱 전 수치(컨테이너
1280/984, 메뉴 간격 32, 메뉴군↔유틸군 80) · 메가메뉴(LNB) 전 수치.

**추가로 알게 된 것 두 개**
- `Background=False` 는 배경을 지우는 게 아니라 **불투명도 0** 이다 — 면은 그대로 있다
- 메가메뉴 이미지의 모서리 컷은 피그마에서 LNB 배경색 삼각형을 얹어 만든다.
  이쪽은 `clip-path` 로 잘랐는데 잘린 자리에 LNB 배경이 그대로 보이므로 결과가 같다 —
  크기·색이 바뀌어도 따라가므로 이대로 둔다


## 2026-09-07 · Navigation 섹션 재구성 (덤프 재내보냄 08:16 UTC)

피그마에서 Navigation 섹션이 다시 만들어져 노드 ID가 `1008:` → `1019:` 로 바뀌었다.

- **푸터 CTA 버튼** `Round=Brand` → `Round=Normal`
- **`Navigation/LNB/List` 신규** (변형 10개) — 메가메뉴 목록 항목이 정식 컴포넌트가 됐다.
  `State=Active`(굵기 500→700)와 `Type=Blank`(외부 링크 ↗)가 정식 상태로 들어왔다
- **메가메뉴 어두운 배경 글자** `#c9ccd0` → `#e4e6e8`
- `GNB/LNB` 에 `Mode` 변형 2개 생김 · `Section_Contact` 가 독립 세트가 되고 기본 숨김(opt-in)으로 바뀜

## 2026-09-07 · 이름 충돌 두 건 확정 (사용자 확인)

덤프에서 같은 스타일 이름에 값이 두 개씩 잡혔던 것을 확정했다.

| 스타일 | 확정값 | 낡은 쪽 |
|---|---|---|
| `Semantic/Label/Normal` | **`#151617`** | `#171719` (key `e05e910d…`) — 아이콘 SVG 원본 벡터 41곳에 남아 있었다 |
| `Semantic/Background/Normal/Alternative` | **`#fafafa`** | `#f5f6f7` (key `363b4272…`) — Content Badge 면 84곳이 이쪽에 묶여 있다 |

- `--semantic-label-normal` 을 `#151617` 로 바꿨다. `--var-label-normal`(#171719) 제거
- `--semantic-background-normal-alternative-b`(#f5f6f7) 제거
- **남은 문제** — 배지가 낡은 쪽에 묶여 있어서, 낡은 참조를 확정값으로 바꾸면
  배지 면이 `#fafafa` 가 되어 흰 배경에서 안 보인다. 배지를 어느 토큰에 묶을지는
  디자인 결정으로 남겼고, 그 전까지 `#f5f6f7` 을 직접 적어 화면을 유지한다

## 2026-09-07 · 토큰 구조를 피그마 3층 체계에 맞춤

색이 피그마에서 `원시 변수 → 의미 스타일 → 노드` 3층으로 관리되고 있는 것을 확인하고,
`tokens.css` 를 그 구조대로 다시 썼다.

- 변수 이름을 **피그마 Semantic 스타일 이름과 1:1** 로 맞춤(`--semantic-label-normal-alternative` 등).
  기존 짧은 이름은 별칭으로 남겨 컴포넌트 CSS 는 그대로 동작한다
- 빠져 있던 의미 토큰 6개 추가 — `Label/Inverse/Strong` · `Background/Inverse/Alternative` ·
  `Background/Inverse/Assistive` · `Line/Inverse/Alternative` · `Label/Normal` · `Line/Normal`
- 텍스트 스타일 13개 → **22개 전부**. `--subtitle-1` 은 실제 이름이 `Title 3/SemiBold` 였다
- 라운드에 `2px` 추가 · opacity 단계 전부 등재

## 2026-09-08 · Definition / Icon / Logo 덤프로 활자·아이콘·로고 정본화

근거 파일: `~/Downloads/{Definition,Icon,Logo}.f2h.json` (`Crissit_Main` 원본에서 내보냄)

### 활자 — 반응형 스케일이 새로 나왔다

`Typography / Definition (5:3334)` 213개 노드를 대조했다. 크기가 화면 폭에 따라
세 단으로 바뀌는 정책이 있는데 그동안 몰랐다 — **활자의 구간(lg ≥1280 · md 768–1279 ·
sm <768)은 컴포넌트의 구간(1280 / 1024 / 768)과 다르다.**

| 단계 | lg | md | sm | 굵기 |
|---|---|---|---|---|
| Display | 64 | 48 | 36 | Extra Bold 800 |
| Heading 1 | 48 | 40 | 30 | Bold 700 |
| Heading 2 | 40 | 32 | 26 | Bold 700 |
| Title 1 | 32 | 24 | 24 | Bold 700 |
| Title 2 | 24 | 20 | 20 | Bold 700 |
| Title 3 | 20 | 18 | 18 | Semi Bold 600 |

- `tokens.css` 에 크기만 `--fs-display`·`--fs-h1`·`--fs-h2`·`--fs-t1`·`--fs-t2`·`--fs-t3` 로
  빼고, 미디어쿼리 두 개가 그 여섯 개만 바꾼다. 굵기·행간을 담은 토큰은 안 건드린다
- `heading.css` 에서 이쪽이 추정으로 넣었던 `font-size:36px`/`28px` 을 걷어냈다 —
  이제 토큰 스케일이 크기를 정한다
- Body 계열에 굵기 4단(Regular·Medium·SemiBold·Bold)과 `Body 2/Reading`·`Body 3` 을 추가
- **정의서 ≠ 실제 스타일 2건** — `Display`(정의서 800·64/125% vs 스타일 `Display 1` 700·32/130%),
  `Body 1/Reading`(정의서 18/175% vs 스타일 16/175%). 토큰은 정의서를 따랐다

### 아이콘 — 라이브러리 전체를 받았다

`Icon (38:4019)` 세트 38개 · 컴포넌트 72개를 전수 추출해 중복을 뺀 **글리프 64종**을
`assets/` 에 넣고 `css/icons.css` 에 `.i-*` 64개를 만들었다. 전에는 11개뿐이었고,
필요할 때마다 시안에서 하나씩 받고 있었다.

| 바로잡은 것 | 전 | 후 |
|---|---|---|
| `Icon/Normal/-` `Name=dummy` 의 정체 | 「자리표시자일 것」이라 적어 둠 | **점선 사각형이 맞다** — 라이브러리에서 직접 확인 |
| 재생(▶) 아이콘 | 「라이브러리에 없다. 추가 요청 필요」 | **있다** — `Icon/Normal/play` 의 `Auto On=True` 가 삼각형이다. 전에 두 변형을 같은 것으로 잘못 읽었다 |
| 이름 ↔ 그림 불일치 | 몰랐다 | `Arrow Down`(38:4115)의 그림은 `+` · `Soft` 두 세트가 서로 다른 그림 · `Roket` 의 변형 이름이 `Name=reset` |

이름은 **그려진 모양대로** 붙였다(`i-plus` · `i-soft`/`i-chip` · `i-rocket` · `i-pause`/`i-play`).

### 로고 — 조각 로고와 두 색 원본을 추가

- 가로형 아트보드는 148×40 인데 실제 그림은 148×32 이고 위아래 4씩 여백이 있다.
  그대로 `mask` 에 쓰면 로고가 작아져서 파일의 `viewBox` 를 `0 4 148 32` 로 잘랐다 —
  그 비율(4.625:1)이 GNB 실측 89×19.2 와 정확히 맞는다
- 심볼만(`.logo-crissit-symbol` 34×32) · 워드마크만(`.logo-crissit-title` 108×25) 추가
- 두 색 원본 `logo-crissit-color.svg` 추가. 단색은 `mask` 한 파일로 계속 처리한다
  (피그마 `Type=Default`/`Dark`/`White` 는 모양이 같고 색만 다르다)

### Progress — 프레임을 걷어냈다

재생(▶) 글리프가 라이브러리에 있다는 것을 확인하면서 두 가지를 같이 정리했다.

- `data-progress="0~4"` 를 **CSS·JS·카탈로그에서 전부 제거**했다.
  피그마 목업의 4장은 피그마가 움직임을 못 그려서 쓴 표현 방식이지 컴포넌트의
  상태가 아니다. 코드에 프레임을 남기면 없는 상태를 하나 만드는 셈이라 지웠다 —
  각도는 그냥 연속으로 돈다
- 카탈로그의 「Progress 정지 상태」 카드(정지 이미지 5장)를 **주기 3초짜리 실동작
  데모**로 바꿨다. 8초를 기다리지 않고 차오르는 것과 일시정지를 볼 수 있다
- 일시정지를 누르면 아이콘이 `i-pause` → `i-play` 로 바뀐다.
  전에는 글리프가 없다고 잘못 알아서 못 하던 것이다

### 아이콘을 24×24 틀에 다시 앉혔다

처음 뽑을 때 **그림 덩어리만** 잘라 냈다. 피그마 아이콘 컴포넌트는 전부 24×24 인데
그 안의 그림 크기는 제각각이라(`blank` 12×12 · `close` 16×16 · `menu` 24×24),
`mask ... contain` 이 덩어리를 상자에 꽉 채우면서 아이콘마다 크기가 어긋났다 —
70개 중 **49개**가 그랬고, 메가메뉴 외부 링크 `↗` 가 67% 크게 보인 게 그 결과다.

각 그림을 원래 자리(`translate(dx,dy)`)에 둔 채 24×24 틀에 다시 앉혔다.
전 64개 파일의 `viewBox` 가 `0 0 24 24` 로 통일됐다.

같이 고친 것 — 카탈로그의 아이콘 목록을 `flex` 에서 격자(`.iconset`)로 바꿨다.
이름 길이가 제각각이라 `guide-desktop` 같은 긴 이름이 두 줄로 넘어가면서
그 칸만 높아져 아이콘 줄이 어긋났다.

## 2026-09-08 · 카탈로그에 검사기를 붙였다

값을 확인하려고 매번 CSS 를 뒤지던 것을 화면에서 바로 보게 만들었다.
`css/inspector.css` · `js/inspector.js` — **카탈로그 전용 문서 UI** 이고
실제 사이트에는 넣지 않는다.

- 마우스가 가리키는 요소의 계산된 값(크기·여백·모서리·글꼴·색·그림자·아이콘)을
  오른쪽 패널에 보여준다
- 그 값이 `tokens.css` 의 어떤 토큰인지 찾아 이름을 붙인다. 같은 색에 이름이
  여럿이면 보고 있는 자리에 맞는 것을 앞에 올린다(글자색이면 `--label-…`)
- 클릭으로 고정 · `↑` `↓` 로 부모·자식 이동 · `Esc` 로 해제
- 조상 사슬을 눌러 위로 올라갈 수 있다 — 버튼을 가리켜도 안쪽 `<span>` 이 잡히므로
- **토큰 이름을 누르면 패널 아래에 정보창**이 열린다(한 번에 하나만). 별칭 사슬
  (`--primary-normal → --semantic-primary-normal → --purple-500 → #543efa`),
  미디어쿼리별 값(`--fs-display` 64/48/36), 같은 값을 가진 다른 토큰,
  색 견본·활자 미리보기. 선언 안의 `var(--…)` 도 눌러서 따라 들어갈 수 있다
- 이름이 넷 이상이면 셋만 펴고 `+3` 으로 접는다

### 만들면서 걸린 것

`document.styleSheets` 를 훑어 `:root` 의 토큰을 모으는데 하나도 안 잡혔다.
`if (r.cssRules) { …@media 로 보고 내려간다… }` 로 갈랐던 게 원인이다 —
**요즘 크롬은 CSS 중첩을 지원해서 보통 규칙도 빈 `cssRules` 를 가진다.**
그래서 전부 @media 로 오인해 건너뛰었다. 규칙을 먼저 읽고 그 다음에 내려가도록 고쳤다.

## 2026-09-08 · 디자이너 메모 · 깃허브 공개

### 메모 — 화면에 핀을 꽂아 남긴다

`css/memo.css` · `js/memo.js` — 검사기와 짝인 **카탈로그 전용 문서 UI**.

- 요소에 핀이 꽂히고, 선택자·구역·그때의 값(색·글꼴·크기 + 토큰 이름)이 같이 저장된다
- 검사 모드를 꺼도 핀은 보인다. "여기 얘기 중" 이 계속 보여야 하기 때문이다
- 서버가 없으므로 `localStorage` 에 쌓고 **JSON 으로 내보내 전달**한다.
  불러오면 핀까지 되살아나고, 같은 `id` 는 덮어써서 두 사람 메모를 합칠 수 있다
- 검사기에 `window.catInspect` 를 열어 메모가 쓰게 했다
  (`selectorPath` · `resolve` · `pin` · `onPin` · `summary`)

**걸린 것** — `setHint()` 가 알림을 먼저 부르고 그 다음 `hint.innerHTML` 을 덮어써서,
메모가 끼워 넣은 「＋ 메모」 버튼이 곧바로 지워졌다. 알림을 innerHTML 뒤로 옮겼다.

### 검사기 — 정보창을 열 때 자동 스크롤

아래쪽 이름을 누르면 정보창이 그 이름을 가렸다. 가려진 만큼만 목록을 밀어 올린다.
`scrollIntoView` 는 바깥 스크롤(본문)까지 움직여 보고 있던 자리를 잃으므로 쓰지 않았다.

### 깃허브 공개

이 폴더만 저장소로 만들어 올렸다. 상위 기획 폴더(사실대장·백로그·회신표)는 올리지 않았다.

- 저장소 <https://github.com/vermil02/crissit-components> (public)
- 공개 주소 <https://vermil02.github.io/crissit-components/>
- `index.html` 은 Pages 진입점이다. 팀이 아는 이름을 지키려고 `카탈로그.html` 을
  그대로 두고 여기서 넘긴다

## 2026-09-08 · 패널 2열 · 표에 값 이름 · 말투

### 검사와 메모를 좌우로 갈랐다

같은 아래 칸을 다투던 것을 열 두 개로 나눴다 — 왼쪽 검사, 오른쪽 메모.
**값을 보면서 메모를 써야** 하기 때문이다.

- 화면 1600px 이상이면 패널이 340 → 680px 로 늘어나 좌우로 나란히 선다
- 그보다 좁으면 한 열로 접어 메모가 아래로 내려온다. 340px 를 좌우로 쪼개면
  둘 다 좁아져서 값도 메모도 못 읽는다
- 메모 창(목록/편집)은 이제 열 안에서 바뀐다. 아래 칸은 토큰 정보창 전용이다

### 타이포 표에 값 이름을 붙였다

`400 18 / 1.55` 는 아는 사람만 읽는 표기였다. **굵기 · 크기 · 행간** 세 열로 가르고,
행간이 글자 크기의 배수라는 것(18px × 1.55 = 27.9px)과 피그마의 % 표기와 같은 값이라는
것을 표 위에 적었다. `Reading` 이 무엇인지도 한 줄 넣었다.

### 말투

디자이너가 보는 화면인데 경고·안내 문구가 서술체였다 —
`"되돌릴 수 없다"` → `"되돌릴 수 없습니다"`. 사람에게 말을 거는 문구(확인창·경고·
힌트·빈 목록·버튼 설명)를 존댓말로 바꿨다. 카탈로그 본문 설명은 문서 서술체를 유지한다.

## 2026-09-08 · 검사·메모 버튼 분리 · 색을 토큰에 마저 묶음

### 버튼을 둘로 갈랐다

한 버튼이 두 열을 같이 열어서 늘 한쪽이 방해가 됐다. 이제 따로 켠다.

- **검사** 켜면 값 열만 (340px) · **메모** 켜면 메모 열만 (340px)
- **둘 다** 켜면 1600px 이상에서 680px 로 넓어져 좌우로 나란히 선다
- 요소 고르기(마우스 따라다니기·클릭 고정)는 둘 중 하나만 켜져도 돈다 —
  메모만 켜고도 화면에서 바로 핀을 꽂을 수 있어야 하기 때문이다
- 메모 버튼에 개수 뱃지를 붙였다. 열지 않아도 메모가 있는지 보인다

### 색을 토큰에 마저 묶었다

컴포넌트 CSS 12개를 전수 확인한 결과 **선언에 직접 쓴 색은 없었다** — 눈에 띄던
`#151617` 같은 값은 전부 주석 안의 실측 기록이었다. 다만 두 군데가 사슬이 끊겨 있었다.

| 곳 | 전 | 후 |
|---|---|---|
| `tokens.css` 그라디언트 5개 | 안에 색을 직접 박아 둠 | 전부 `var(--neutral-…)` · `var(--purple-500)` 로 팔레트를 가리킨다 |
| 그라디언트 전용 두 색 | 이름 없이 `#2977FD` `#7E29FD` | `--blue-500` · `--violet-500` 로 팔레트에 올렸다 |

이제 **팔레트 한 곳만 고치면 그라디언트도 따라오고**, 검사기에서도 이름으로 읽힌다.
그 두 색은 피그마에 변수가 없어 이쪽에서 이름을 붙인 것이라, 「확인 필요」와
디자이너 문서에 올려 두었다.

남은 직접 색 둘은 일부러 그대로 뒀다.
- `boardlist.css` 마스크의 `#000`·`#0000` — 색이 아니라 마스크의 불투명/투명이다
- `inspector.css`·`memo.css` — 컴포넌트가 아닌 문서 UI 다. 그래도 검사기 강조색은
  `var(--purple-500)` 을 가리키게 했고, 메모 색은 `--memo-accent` 하나로 모았다

### 비활성 버튼이 안 잡히던 것 — 덮개로 고름

`disabled` 버튼은 브라우저가 **클릭 이벤트를 아예 만들지 않는다.** 그래서
`document` 에서 클릭을 가로채 고정하던 방식으로는 비활성 버튼을 고를 수 없었다.

검사 중에는 화면 전체를 투명한 판(`.insp-catch`, z-index 9994)으로 덮고,
그 판이 마우스를 받은 뒤 좌표로 아래 요소를 찾는다(`elementsFromPoint`).
핀(9995)·테두리(9996)·패널(9997)·버튼(9998)은 판 위에 있어 그대로 눌린다.

같이 풀린 것 둘
- 페이지의 버튼·링크가 **눌리지 않는다**. 전에는 `preventDefault` 로 막았는데
  비활성 요소에는 그 방법이 애초에 닿지 않았다
- 마우스가 요소에 닿지 않으니 **평소 값**이 나온다. 전에는 hover 값이 나와서
  `Button/Text` 굵기가 700 으로 보였다. 대신 hover 상태는 검사기로 못 본다

## 2026-09-08 · 카탈로그 설명글을 껐다 켤 수 있게

값의 출처·판단 근거를 적은 글(`.desc` `.src` `.note` `.spec`)이 컴포넌트마다
붙어 있는데, **모양만 보려는 사람에게는 방해**였다. 오른쪽 아래 「설명」 버튼으로
껐다 켠다 — `js/annotations.js`.

- **기본은 꺼짐.** 처음 여는 사람은 컴포넌트만 깔끔하게 본다
- 선택은 `localStorage` 에 남아 페이지를 옮겨도 유지된다
- 깜빡임을 막으려고 `카탈로그.html` 의 `<html>` 에 `hide-notes` 를 미리 박고,
  스크립트가 「켬」일 때만 뗀다
- 행 이름(`.lbl` — `Disable = False / True` 같은 것)은 안 숨긴다.
  그건 설명이 아니라 그 줄이 무엇을 보여주는지 알려주는 내용이다

### 설명글을 세 층으로 다시 갈랐다

처음에는 `.desc` `.src` `.note` `.spec` 을 통째로 감췄는데, **그건 틀렸다** —
`.spec` 에 치수가 들어 있어서 개발에 필요한 값까지 같이 사라졌다.

성격으로 다시 갈랐다.

| 층 | 무엇 | 보임 |
|---|---|---|
| `.desc` `.note` `.spec` | 정의 · 동작/정책 · 치수·색 | 항상 |
| `.src` `.aside` | 피그마 출처 · 근거와 이력 | 「설명」으로 켬 |

작업 로그성 문장 14개를 `.aside` 로 옮겼다 — "이름을 그대로 옮겼다",
"피그마에 변형이 없어 이쪽에서 정했다", "덤프에 나오지만 토큰이 아니다",
"피그마 목업 4장은 …", "viewBox 를 잘라 뒀다" 같은 것들이다.
치수와 근거가 한 문단에 섞여 있던 다섯 곳은 문단을 쪼갰다.

`.aside` 는 왼쪽에 세로선을 두어 본문과 구분한다.

**앞으로 새 문단을 쓸 때** — *"이 문장이 없으면 잘못 구현되나?"* 를 묻는다.
그렇다면 `.note` 나 `.spec`, 아니면 `.aside` 이거나 이 변경기록으로 간다.

### 2열 기준을 1600 → 1400px 으로 낮췄다

다른 PC 에서 메모가 아래로 내려온다는 이야기가 나왔다. 2열 기준을 1600px 로
잡아 둔 탓이다 — 노트북이 대개 1440(13" Air)·1512(14") 라 대부분 걸렸다.

- **1400px 이상** 좌우로 나란히 (패널 680)
- **1400px 미만** 위아래로 반씩 (패널 340). 1366 이하 노트북이 여기다

쌓일 때 메모 칸이 머리·발에 눌려 목록이 안 보이던 것도 같이 고쳤다 —
높이를 내용에 맡기지 않고 `grid-template-rows:1fr 1fr` 로 **정확히 반씩** 준다.

## 2026-09-08 · 이름을 줄여 쓰지 않는다

### 아이콘 목록의 `·-thick` 을 풀었다

`check` 다음 칸에 `·-thick` 이라고 적어 뒀는데, 앞 이름과 이어 읽어야 뜻이 통하는
표기였다. **줄 바뀜·정렬이 달라지면 무엇의 thick 인지 알 수 없다.**

64개 라벨을 **실제로 써야 하는 클래스 그대로** 바꿨다 — `.i-check-thick` 처럼.
가장 긴 이름(`.i-chevron-right-sm-thick`)이 한 줄에 들어가게 칸을 152px 로 잡고
줄바꿈을 막았다. 로고도 같이 폈다(`·-symbol` → `.logo-crissit-symbol`).

### 같은 김에 찾은 것 — 버튼 축 값의 철자가 갈려 있다

| 철자 | 세트 |
|---|---|
| `Assitive` (s 하나 빠짐) | `Button/Solid` 15 · `Button/Text` 11 · `Button/Icon/Solid` 16 |
| `Assistive` (맞는 철자) | `Button/Outlined` 12 · `Info_btn` 1 |

같은 축 값이 두 철자로 갈려 있어 세트를 바꿔 끼울 때 변형이 안 맞는다.
코드는 처음부터 맞는 철자를 썼고(`.btn--assistive`), 카탈로그 표기도 통일했다.
「확인 필요」와 디자이너 문서에 올렸다.

### 같이 확인한 것 (문제 없음)

- `css/icons.css` — 클래스 `.i-X` ↔ 파일 `icon-X.svg` 64쌍 전부 일치
- 행 이름(`.lbl`) 14개 — 전부 스스로 읽히는 이름이다(`Disable = False / True` 등)
